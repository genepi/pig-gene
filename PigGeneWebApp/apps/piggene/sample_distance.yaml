!cloudgene.mapred.apps.App
name: sample_distance
category: Piggene
description: Calculates the distance between two locations (given by latitude and longitude) using the
   haversine formula.
mapred: 
   outputs: 
   -  id: output1
      description: output1
      mergeOutput: false
      type: hdfs-folder
      zip: false
   steps: 
   -  name: PigScript
      params: -param output1=$output1
      pig: sample_distance.pig
version: 0.1.0
